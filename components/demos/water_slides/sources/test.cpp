/*#include <stdio.h>

#include "shared.h"

const char* configuration_name = "data/configuration.xml";

int main ()
{
  Configuration configuration (configuration_name);

  printf ("WindowWidth = '%s'\n", configuration.StringProperty ("WindowWidth"));

  return 0;
}
*/