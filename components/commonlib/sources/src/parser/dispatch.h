namespace common
{

//implementation forwards
class ParseContext;

///////////////////////////////////////////////////////////////////////////////////////////////////
///Диспетчер разбора
///////////////////////////////////////////////////////////////////////////////////////////////////
class ParseDispatch
{
  public:
///////////////////////////////////////////////////////////////////////////////////////////////////
///Разбор с указанием формата или с его автоопределением
///////////////////////////////////////////////////////////////////////////////////////////////////
    static void Parse (const char*   file_name,
                       ParseContext& context,
                       const char*   format);
    static void Parse (const char*   name,
                       const char*   buf,
                       size_t        size,
                       ParseContext& conext,
                       const char*   format);

///////////////////////////////////////////////////////////////////////////////////////////////////
///Функции разбора конкретных форматов
///////////////////////////////////////////////////////////////////////////////////////////////////
    static void ParseWXF (char* buf,ParseContext& context);
    static void ParseXML (char* buf,ParseContext& context);
};

}
