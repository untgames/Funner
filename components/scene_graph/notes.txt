1. Список контроллеров, линкуемый в сцену
2. Сериализатор
  - обобщение по имени типа узла

Node не зависит от SceneManager
Добавить Entity::IsVisible/SetVisible

Таймеры:
  - таймеры хранятся в сцене, есть доступ по имени
  - для узла по умолчанию проассоциирован таймер "default" (ищется в сцене)
  - можно установить кастомный таймер

Убрать Node::Update, добавить Scene::Update (без времени)

Возможно сделать один таймер на сцену и Scene::Update (t, dt)

SceneManager::PlayAnimation (const char* name, Node& root_node)

Trigger
InputTrigger
  - eventType -> action
  - geometry
  
Этапы:
1. Сделать контроллер, аналог main.lua EvalSpriteState
2. Сделать загрузку данных для контроллера из XML
  
------------------------------------------------------------------

1. ResourceList, хранимый в Node. Построение полного списка по дочерним узлам
2. Node не зависит от SceneManager
3. ParticleSystem при создании принимает ParticleManager
4. AnimationController при создании принимает AnimationManager
5. ?Создание  узлов  при  помощи  SceneManager,  которые  требуют  передачи  в
конструктор  кастомного  менеджера   ->   регистрация   кастомных   загрузчиков
непосредственно в SceneManager (возможность использования bind+function)
6. ?Загрузка ресурсов в AnimationManager и ParticleManager>>>>>>> .r6401
